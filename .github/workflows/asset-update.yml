name: Update WordPress.org assets

# Publishes plugin directory assets (icon, banner, screenshots) to the
# WordPress.org SVN /assets/ directory WITHOUT needing a version bump.
# Runs whenever files under .wordpress-org/ change on main, or manually.
#
# Uses the same secrets as the deploy workflow:
#   SVN_USERNAME, SVN_PASSWORD

on:
  push:
    branches: [main]
    paths:
      - '.wordpress-org/**'
      - 'readme.txt'
  workflow_dispatch: {}

# Shared with the deploy workflow so the two never write to SVN at the same time.
concurrency:
  group: wporg-svn
  cancel-in-progress: false

jobs:
  assets:
    name: Sync assets to WordPress.org
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Update assets
        uses: 10up/action-wordpress-plugin-asset-update@stable
        env:
          SVN_USERNAME: ${{ secrets.SVN_USERNAME }}
          SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}
          SLUG: ag-data-for-posthog
          ASSETS_DIR: .wordpress-org
          # Sync only readme.txt + assets to trunk; ignore code/CI changes in
          # the same push (those ship via the version-tagged deploy workflow).
          IGNORE_OTHER_FILES: true
